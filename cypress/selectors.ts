const auth = {
	form: {
		email: 'input#email',
		password: 'input#password',
		submit: '#sign-in',
	},
	// signOutDropdownButtonId: '#signout-icon-button',
	// signOutEntryButtonId: '#signout-entry',
};

const selectors = {
	auth,
};

export default selectors;
