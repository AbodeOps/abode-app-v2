import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myabode.app',
  appName: 'Abode',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
